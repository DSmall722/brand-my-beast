# Local preview

Slice **16.8**. `npm i && npm run dev` is how friends see the real page while Vercel is on hold.

The Next.js app in this repo is the page. Not the static prototype. Not brandmybeast.com as a demo while the hold is on. See `docs/VERCEL-HOLD.md`.

## Look at it

```bash
npm i && npm run dev
```

Open the URL the dev server prints (default `http://localhost:3000`). That is the real page.

Floor **$58,000**. Buyout **$120,000**. `CLOSE_AT` stays null. No Stripe. No card charge. No close date.

Public strings only: BrandMyBeast, @BrandMyBeast, hello@brandmybeast.com.
