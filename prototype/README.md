# Static prototype

Look reference only. Bids are `localStorage`. This is not the auction.

```bash
npx serve prototype
```

Money in `app.js` must stay `FLOOR = 58000` and `GOAL = 120000`. Do not deploy this folder to brandmybeast.com.

Truck JPGs (`assets/hero.jpg` and side/front/rear/etch) live on the working machine if they did not land in this commit.
